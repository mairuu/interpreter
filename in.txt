trait Iterable {
  has_next
  next
}

struct Range {
  start
  end
  current
}

impl Iterable for Range {
  fun has_next(self) { return self.current < self.end }
  fun next(self) { return self.current = self.current + 1 }
}